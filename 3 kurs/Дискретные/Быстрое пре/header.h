#include <iostream>
#include <vector>
#include <math.h>
#include <bitset>

using namespace std;

const int n = 5;
const int l = pow(2, n);

// f - vector val
// B matrix
vector<float> BPF(vector<float> f, vector<vector<float>> B);
